@model IEnumerable<CaseStudy_19_1_23_.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CAName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CAName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CAId }, new { @class = "btn btn-info" }) |
            @Html.ActionLink("Details", "Details", new { id=item.CAId }, new { @class = "btn btn-warning" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CAId }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
